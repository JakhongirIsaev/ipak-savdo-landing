# Copy packages instead of hardlinking them from the pnpm store.
# This project lives under a OneDrive-synced folder, where hardlinks get broken
# by the sync engine and cause "Cannot find module" errors on build/test.
# Copy mode is slightly slower to install but immune to that breakage.
package-import-method=copy
